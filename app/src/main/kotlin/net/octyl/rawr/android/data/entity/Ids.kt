package net.octyl.rawr.android.data.entity

// Later this can become inline classes
// For now we must "wrap" it.

data class SongId(val id: RawrId)
data class TagId(val id: RawrId)
data class PlaylistId(val id: RawrId)
